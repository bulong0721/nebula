package ${package_name}.api;

import com.rhea.common.base.BaseServiceMock;
import ${package_name}.dao.mapper.${model}Mapper;
import ${package_name}.dao.model.${model};

/**
* 降级实现${model}Service接口
* Created by rhea on ${ctime}.
*/
public class ${model}ServiceMock extends BaseServiceMock<${model}Mapper, ${model}> implements ${model}Service {

}
